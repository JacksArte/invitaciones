<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Invitación cumpleaños 56 de Juana</title>
  <style>
    :root {
      --rosa: #ffd7e8;
      --rosa-fuerte: #d978a7;
      --lavanda: #dcd6ff;
      --menta: #d9f8e9;
      --cielo: #dff3ff;
      --durazno: #ffe3c9;
      --crema: #fff8ed;
      --texto: #6a4d5b;
      --texto-suave: #8a6c79;
      --sombra: rgba(118, 73, 91, .23);
      --blanco: rgba(255,255,255,.86);
    }

    * { box-sizing: border-box; }

    html, body { min-height: 100%; }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      color: var(--texto);
      overflow-x: hidden;
      background:
        radial-gradient(circle at 12% 12%, rgba(255, 215, 232, .95), transparent 26%),
        radial-gradient(circle at 88% 14%, rgba(220, 214, 255, .95), transparent 27%),
        radial-gradient(circle at 12% 86%, rgba(217, 248, 233, .94), transparent 29%),
        radial-gradient(circle at 90% 86%, rgba(255, 227, 201, .94), transparent 28%),
        linear-gradient(135deg, #fff8ed 0%, #fff0f8 46%, #f3f0ff 100%);
    }

    .sky {
      position: fixed;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
      z-index: 0;
    }

    .balloon {
      --size: 70px;
      position: absolute;
      bottom: -150px;
      width: var(--size);
      height: calc(var(--size) * 1.22);
      border-radius: 50% 50% 48% 48%;
      background:
        radial-gradient(circle at 30% 24%, rgba(255,255,255,.78) 0 10%, transparent 11%),
        linear-gradient(145deg, var(--b1), var(--b2));
      box-shadow: inset -10px -14px 18px rgba(128, 77, 94, .13), 0 18px 26px rgba(128, 77, 94, .13);
      opacity: .88;
      animation: fly var(--dur) linear infinite;
      transform: translateX(0) rotate(var(--rot));
      pointer-events: auto;
      cursor: pointer;
    }

    .balloon::before {
      content: "";
      position: absolute;
      left: 50%;
      bottom: -10px;
      transform: translateX(-50%);
      width: 0;
      height: 0;
      border-left: 8px solid transparent;
      border-right: 8px solid transparent;
      border-top: 14px solid var(--b2);
      filter: brightness(.97);
    }

    .balloon::after {
      content: "";
      position: absolute;
      left: 50%;
      bottom: -92px;
      width: 2px;
      height: 88px;
      background: linear-gradient(var(--texto-suave), transparent);
      opacity: .38;
      transform-origin: top;
      animation: stringWave 2.8s ease-in-out infinite;
    }

    .b1 { --b1:#ffd7e8; --b2:#f5a8c7; --size:76px; --dur:17s; --rot:-8deg; left:8%; animation-delay:-2s; }
    .b2 { --b1:#dcd6ff; --b2:#aaa0ef; --size:62px; --dur:20s; --rot:7deg; left:22%; animation-delay:-8s; }
    .b3 { --b1:#d9f8e9; --b2:#9cd8b8; --size:82px; --dur:22s; --rot:5deg; left:72%; animation-delay:-5s; }
    .b4 { --b1:#ffe3c9; --b2:#ffc08e; --size:66px; --dur:18s; --rot:-5deg; left:86%; animation-delay:-11s; }
    .b5 { --b1:#dff3ff; --b2:#9ccfe9; --size:58px; --dur:19s; --rot:11deg; left:50%; animation-delay:-13s; }
    .b6 { --b1:#fff3b8; --b2:#f4d876; --size:54px; --dur:21s; --rot:-12deg; left:38%; animation-delay:-4s; }

    @keyframes fly {
      0% { transform: translateY(0) translateX(0) rotate(var(--rot)); }
      35% { transform: translateY(-42vh) translateX(34px) rotate(calc(var(--rot) * -1)); }
      70% { transform: translateY(-84vh) translateX(-24px) rotate(var(--rot)); }
      100% { transform: translateY(-125vh) translateX(26px) rotate(calc(var(--rot) * -1)); }
    }

    @keyframes stringWave {
      0%,100% { transform: translateX(-50%) rotate(-5deg); }
      50% { transform: translateX(-50%) rotate(6deg); }
    }

    .sparkle {
      position: absolute;
      font-size: 20px;
      opacity: .7;
      animation: twinkle 2.5s ease-in-out infinite;
    }

    .s1 { top: 11%; left: 14%; animation-delay: .2s; }
    .s2 { top: 21%; right: 16%; animation-delay: 1s; }
    .s3 { bottom: 18%; left: 18%; animation-delay: 1.7s; }
    .s4 { bottom: 15%; right: 12%; animation-delay: .6s; }
    .s5 { top: 52%; left: 7%; animation-delay: 1.3s; }
    .s6 { top: 49%; right: 8%; animation-delay: .9s; }

    @keyframes twinkle {
      0%, 100% { transform: scale(.82) rotate(0deg); opacity: .45; }
      50% { transform: scale(1.18) rotate(9deg); opacity: .95; }
    }

    .wrap {
      position: relative;
      z-index: 2;
      min-height: 100vh;
      display: grid;
      place-items: center;
      padding: 30px 16px;
    }

    .invite {
      width: min(94vw, 520px);
      min-height: 720px;
      position: relative;
      perspective: 1400px;
      border-radius: 38px;
    }

    .card {
      position: relative;
      min-height: 720px;
      border-radius: 38px;
      box-shadow: 0 26px 70px var(--sombra);
      transform-style: preserve-3d;
      transition: transform 1s cubic-bezier(.2,.72,.18,1);
      cursor: pointer;
    }

    .invite.open .card { transform: rotateY(180deg); }

    .front, .inside {
      position: absolute;
      inset: 0;
      backface-visibility: hidden;
      border-radius: 38px;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,.78);
    }

    .front {
      display: grid;
      place-items: center;
      padding: 34px;
      text-align: center;
      background:
        linear-gradient(135deg, rgba(255,255,255,.86), rgba(255,247,251,.72)),
        radial-gradient(circle at 12% 12%, rgba(255, 215, 232, .92), transparent 31%),
        radial-gradient(circle at 92% 20%, rgba(220, 214, 255, .9), transparent 32%),
        radial-gradient(circle at 78% 88%, rgba(217, 248, 233, .85), transparent 35%);
    }

    .inside {
      transform: rotateY(180deg);
      background:
        linear-gradient(160deg, rgba(255,255,255,.92), rgba(255,248,238,.88)),
        radial-gradient(circle at 18% 13%, rgba(255,215,232,.82), transparent 30%),
        radial-gradient(circle at 86% 18%, rgba(220,214,255,.78), transparent 31%),
        radial-gradient(circle at 50% 92%, rgba(217,248,233,.76), transparent 36%);
      padding: 28px;
    }

    .front::before,
    .inside::before {
      content: "";
      position: absolute;
      inset: 16px;
      border: 2px dashed rgba(217, 126, 166, .36);
      border-radius: 30px;
      pointer-events: none;
    }

    .front-content, .inside-content {
      position: relative;
      z-index: 3;
    }

    .mini-balloons {
      display: flex;
      justify-content: center;
      align-items: flex-end;
      gap: 12px;
      min-height: 150px;
      margin-bottom: 18px;
    }

    .mini-balloon {
      position: relative;
      width: 58px;
      height: 72px;
      border-radius: 50% 50% 48% 48%;
      background: linear-gradient(145deg, var(--mb1), var(--mb2));
      box-shadow: inset -8px -11px 14px rgba(101,72,87,.12), 0 12px 20px rgba(101,72,87,.12);
      animation: bob 2.4s ease-in-out infinite;
    }

    .mini-balloon:nth-child(2) { width: 72px; height: 90px; animation-delay: .4s; }
    .mini-balloon:nth-child(3) { animation-delay: .8s; }
    .mini-balloon::after {
      content: "";
      position: absolute;
      left: 50%;
      bottom: -52px;
      width: 2px;
      height: 54px;
      background: rgba(106,77,91,.28);
    }

    @keyframes bob {
      0%,100% { transform: translateY(0) rotate(-2deg); }
      50% { transform: translateY(-12px) rotate(3deg); }
    }

    .eyebrow {
      display: inline-block;
      padding: 9px 17px;
      border-radius: 999px;
      font-size: 13px;
      letter-spacing: .16em;
      text-transform: uppercase;
      color: #9a5e7a;
      background: rgba(255, 255, 255, .62);
      box-shadow: 0 10px 24px rgba(128,77,94,.08);
      margin-bottom: 16px;
    }

    h1 {
      margin: 0;
      line-height: .92;
      font-size: clamp(56px, 15vw, 94px);
      font-family: Georgia, "Times New Roman", serif;
      color: var(--rosa-fuerte);
      text-shadow: 0 7px 0 rgba(255,215,232,.72);
    }

    .age {
      margin: 18px auto;
      width: 132px;
      height: 132px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background:
        radial-gradient(circle at 32% 28%, rgba(255,255,255,.78), transparent 18%),
        linear-gradient(145deg, #ffd7e8, #dcd6ff);
      color: white;
      font-weight: 900;
      font-size: 54px;
      box-shadow: 0 17px 32px rgba(128,77,94,.18);
      border: 8px solid rgba(255,255,255,.56);
      animation: pulse 2.8s ease-in-out infinite;
    }

    @keyframes pulse {
      0%,100% { transform: scale(1); }
      50% { transform: scale(1.045); }
    }

    .front-text {
      max-width: 360px;
      margin: 0 auto 20px;
      font-size: 18px;
      color: var(--texto-suave);
      line-height: 1.55;
    }

    .tap-note {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 18px;
      border-radius: 999px;
      background: rgba(255,255,255,.68);
      box-shadow: 0 12px 28px rgba(128,77,94,.09);
      font-weight: 700;
      color: #a45d80;
      animation: softGlow 1.8s ease-in-out infinite;
    }

    @keyframes softGlow {
      0%,100% { transform: translateY(0); box-shadow: 0 12px 28px rgba(128,77,94,.09); }
      50% { transform: translateY(-4px); box-shadow: 0 18px 34px rgba(128,77,94,.16); }
    }

    .floral-corner {
      position: absolute;
      width: 128px;
      height: 128px;
      z-index: 2;
      pointer-events: none;
    }

    .floral-corner span {
      position: absolute;
      filter: drop-shadow(0 7px 8px rgba(180, 95, 134, .14));
      animation: sway 3.3s ease-in-out infinite;
    }

    .top-left { top: 18px; left: 18px; }
    .top-right { top: 18px; right: 18px; transform: scaleX(-1); }
    .bottom-left { bottom: 18px; left: 18px; transform: scaleY(-1); }
    .bottom-right { bottom: 18px; right: 18px; transform: scale(-1); }

    .f1 { top: 22px; left: 34px; font-size: 34px; }
    .f2 { top: 52px; left: 70px; font-size: 32px; animation-delay: .4s; }
    .f3 { top: 78px; left: 28px; font-size: 28px; animation-delay: .8s; }
    .leaf1 { top: 16px; left: 74px; font-size: 24px; animation-delay: .2s; }
    .leaf2 { top: 52px; left: 8px; font-size: 24px; animation-delay: .6s; }

    @keyframes sway {
      0%,100% { transform: rotate(-6deg); }
      50% { transform: rotate(8deg); }
    }

    .inside-content {
      min-height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      gap: 16px;
      padding: 26px 16px;
    }

    .subtitle {
      margin: 0;
      font-size: 17px;
      letter-spacing: .18em;
      text-transform: uppercase;
      color: #9a5e7a;
      font-weight: 700;
    }

    .name {
      margin: 0;
      font-family: Georgia, "Times New Roman", serif;
      font-size: clamp(64px, 16vw, 98px);
      line-height: .88;
      color: var(--rosa-fuerte);
      text-shadow: 0 7px 0 rgba(255,215,232,.7);
    }

    .message {
      max-width: 390px;
      margin: 0 auto;
      font-size: 18px;
      line-height: 1.55;
      color: var(--texto-suave);
    }

    .details {
      width: min(100%, 385px);
      display: grid;
      grid-template-columns: 1fr;
      gap: 12px;
      margin: 8px 0;
    }

    .detail {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 14px 16px;
      border-radius: 22px;
      background: rgba(255,255,255,.64);
      box-shadow: 0 12px 25px rgba(128,77,94,.08);
      text-align: left;
      border: 1px solid rgba(255,255,255,.72);
    }

    .icon {
      width: 44px;
      height: 44px;
      flex: 0 0 44px;
      display: grid;
      place-items: center;
      border-radius: 50%;
      background: linear-gradient(145deg, rgba(255,215,232,.72), rgba(220,214,255,.72));
      font-size: 22px;
    }

    .detail strong {
      display: block;
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: .12em;
      color: #a45d80;
    }

    .detail span {
      display: block;
      margin-top: 2px;
      font-size: 17px;
      color: var(--texto);
      font-weight: 650;
    }

    .cake {
      margin-top: 5px;
      font-size: 48px;
      animation: cakeDance 2.2s ease-in-out infinite;
    }

    @keyframes cakeDance {
      0%,100% { transform: rotate(-3deg) translateY(0); }
      50% { transform: rotate(3deg) translateY(-7px); }
    }

    .closing {
      font-family: Georgia, "Times New Roman", serif;
      font-size: 22px;
      color: #a45d80;
      margin: 0;
    }

    .micro {
      font-size: 13px;
      color: #9b7c89;
      margin-top: 2px;
    }

    .petal, .confetti {
      position: fixed;
      z-index: 8;
      pointer-events: none;
      animation: fall 1.5s ease-out forwards;
    }

    .petal { font-size: 20px; }
    .confetti {
      width: 10px;
      height: 14px;
      border-radius: 4px;
      background: var(--c);
    }

    @keyframes fall {
      0% { transform: translate(0, 0) rotate(0deg) scale(1); opacity: 1; }
      100% { transform: translate(var(--x), var(--y)) rotate(420deg) scale(.8); opacity: 0; }
    }

    .pop {
      animation: pop .34s ease-out forwards !important;
    }

    @keyframes pop {
      0% { transform: scale(1); opacity: 1; }
      70% { transform: scale(1.22); opacity: .75; }
      100% { transform: scale(0); opacity: 0; }
    }

    .hint {
      position: fixed;
      left: 50%;
      bottom: 18px;
      transform: translateX(-50%);
      z-index: 5;
      width: min(92vw, 390px);
      padding: 12px 16px;
      border-radius: 999px;
      background: rgba(255,255,255,.74);
      box-shadow: 0 14px 32px rgba(128,77,94,.11);
      text-align: center;
      color: #9a5e7a;
      font-size: 14px;
      backdrop-filter: blur(8px);
    }

    .invite.open + .hint-open { opacity: 0; }

    @media (max-width: 520px) {
      .invite, .card { min-height: 675px; }
      .front, .inside { border-radius: 30px; }
      .inside { padding: 18px; }
      .front { padding: 24px; }
      .floral-corner { width: 102px; height: 102px; }
      .f1 { top: 18px; left: 28px; font-size: 28px; }
      .f2 { top: 44px; left: 58px; font-size: 27px; }
      .f3 { top: 64px; left: 24px; font-size: 24px; }
      .leaf1 { top: 14px; left: 60px; font-size: 21px; }
      .leaf2 { top: 43px; left: 7px; font-size: 21px; }
      .message { font-size: 16px; }
      .detail span { font-size: 15px; }
      .hint { font-size: 13px; }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation-duration: .001ms !important; animation-iteration-count: 1 !important; transition-duration: .001ms !important; }
    }
  </style>
</head>
<body>
  <div class="sky" aria-hidden="true">
    <div class="balloon b1" title="Toca para explotar el globo"></div>
    <div class="balloon b2" title="Toca para explotar el globo"></div>
    <div class="balloon b3" title="Toca para explotar el globo"></div>
    <div class="balloon b4" title="Toca para explotar el globo"></div>
    <div class="balloon b5" title="Toca para explotar el globo"></div>
    <div class="balloon b6" title="Toca para explotar el globo"></div>
    <span class="sparkle s1">✨</span>
    <span class="sparkle s2">🌸</span>
    <span class="sparkle s3">✨</span>
    <span class="sparkle s4">🌷</span>
    <span class="sparkle s5">🌼</span>
    <span class="sparkle s6">✨</span>
  </div>

  <main class="wrap">
    <section class="invite" id="invite" aria-label="Invitación de cumpleaños de Juana">
      <article class="card" id="card" tabindex="0" role="button" aria-expanded="false" aria-label="Abrir invitación">
        <div class="front">
          <div class="floral-corner top-left" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>
          <div class="floral-corner top-right" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>
          <div class="floral-corner bottom-left" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>
          <div class="floral-corner bottom-right" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>

          <div class="front-content">
            <div class="mini-balloons" aria-hidden="true">
              <span class="mini-balloon" style="--mb1:#ffd7e8;--mb2:#f5a8c7"></span>
              <span class="mini-balloon" style="--mb1:#dcd6ff;--mb2:#aaa0ef"></span>
              <span class="mini-balloon" style="--mb1:#d9f8e9;--mb2:#9cd8b8"></span>
            </div>
            <span class="eyebrow">Estás invitado</span>
            <h1>Juana</h1>
            <div class="age">56</div>
            <p class="front-text">Una tarde especial llena de alegría, flores, globos y mucho cariño.</p>
            <span class="tap-note">💌 Toca para abrir</span>
          </div>
        </div>

        <div class="inside">
          <div class="floral-corner top-left" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>
          <div class="floral-corner top-right" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>
          <div class="floral-corner bottom-left" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>
          <div class="floral-corner bottom-right" aria-hidden="true"><span class="f1">🌸</span><span class="f2">🌷</span><span class="f3">🌼</span><span class="leaf1">🌿</span><span class="leaf2">🍃</span></div>

          <div class="inside-content">
            <p class="subtitle">Cumpleaños 56</p>
            <h2 class="name">Juana</h2>
            <p class="message">Con mucha alegría queremos compartir contigo este día tan especial. Tu presencia hará más bonita la celebración.</p>

            <div class="details">
              <div class="detail">
                <div class="icon">📅</div>
                <div><strong>Fecha</strong><span>Sábado 27 de junio</span></div>
              </div>
              <div class="detail">
                <div class="icon">🕒</div>
                <div><strong>Hora</strong><span>3:00 p.m.</span></div>
              </div>
              <div class="detail">
                <div class="icon">📍</div>
                <div><strong>Lugar</strong><span>Arteaga 30, Palmillas, San Juan del Río</span></div>
              </div>
            </div>

            <div class="cake" aria-hidden="true">🎂</div>
            <p class="closing">¡Te esperamos con mucho cariño!</p>
            <p class="micro">Toca cualquier parte de la tarjeta para lanzar confeti pastel.</p>
          </div>
        </div>
      </article>
    </section>
  </main>

  <div class="hint" id="hint">También puedes tocar los globos para hacerlos explotar 🎈</div>

  <script>
    const invite = document.getElementById('invite');
    const card = document.getElementById('card');
    const hint = document.getElementById('hint');
    let opened = false;

    const colors = ['#ffd7e8', '#dcd6ff', '#d9f8e9', '#ffe3c9', '#dff3ff', '#fff3b8'];
    const flowers = ['🌸', '🌷', '🌼', '✨', '💮'];

    function openInvite() {
      if (!opened) {
        opened = true;
        invite.classList.add('open');
        card.setAttribute('aria-expanded', 'true');
        setTimeout(() => {
          if (hint) hint.textContent = 'Toca la tarjeta para lanzar confeti pastel ✨';
        }, 700);
        burst(window.innerWidth / 2, window.innerHeight / 2, 36);
      }
    }

    function burst(x, y, amount = 24) {
      for (let i = 0; i < amount; i++) {
        const piece = document.createElement('span');
        const isFlower = Math.random() > .58;
        piece.className = isFlower ? 'petal' : 'confetti';
        piece.style.left = x + 'px';
        piece.style.top = y + 'px';
        piece.style.setProperty('--x', (Math.random() * 300 - 150) + 'px');
        piece.style.setProperty('--y', (Math.random() * 240 + 80) + 'px');
        piece.style.animationDelay = (Math.random() * .15) + 's';
        if (isFlower) {
          piece.textContent = flowers[Math.floor(Math.random() * flowers.length)];
        } else {
          piece.style.setProperty('--c', colors[Math.floor(Math.random() * colors.length)]);
        }
        document.body.appendChild(piece);
        setTimeout(() => piece.remove(), 1700);
      }
    }

    card.addEventListener('click', (event) => {
      openInvite();
      burst(event.clientX || window.innerWidth / 2, event.clientY || window.innerHeight / 2, opened ? 20 : 36);
    });

    card.addEventListener('keydown', (event) => {
      if (event.key === 'Enter' || event.key === ' ') {
        event.preventDefault();
        openInvite();
        burst(window.innerWidth / 2, window.innerHeight / 2, 28);
      }
    });

    document.querySelectorAll('.balloon').forEach((balloon) => {
      balloon.addEventListener('click', (event) => {
        event.stopPropagation();
        const rect = balloon.getBoundingClientRect();
        balloon.classList.add('pop');
        burst(rect.left + rect.width / 2, rect.top + rect.height / 2, 18);
        setTimeout(() => {
          balloon.classList.remove('pop');
          balloon.style.animationDelay = '0s';
        }, 900);
      });
    });

    setInterval(() => {
      const x = Math.random() * window.innerWidth;
      const y = Math.random() * (window.innerHeight * .65);
      if (opened) burst(x, y, 6);
    }, 4200);
  </script>
</body>
</html>
